---
title: Updates
# Staged, not published. Delete this build block (and the cascade below it) to
# put /updates/ live — the footer link reappears on its own, because it follows
# whether this page has a URL. Nothing else needs to change.
build:
  render: never
  list: never
cascade:
  build:
    render: never
    list: never
---

When something on the site gets better for readers, a short note shows up here. This is not a list of posts — just what changed, in plain language.
