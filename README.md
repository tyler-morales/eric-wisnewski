# Eric Wisnewski

A forever-stable Hugo blog: no JavaScript frameworks, no CSS frameworks, no Node.js. Vanilla CSS and HTML only.

- **Preview:** `hugo server`
- **Production build:** `hugo`

Deploy the `public/` directory to any static host (GitHub Pages, Netlify, etc.).
